# Local agent service — no Band.ai SDK. Agents communicate over plain HTTP I/O.

# Company LLM provider: native Google Gemini API
google-genai>=1.0.0

# Local HTTP service (replaces the Band WebSocket mesh)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0
pydantic>=2.0.0
rich>=13.0.0
